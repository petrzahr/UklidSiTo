# ==============================================================================
# UklidSiTo – Environment Configuration Template
# ==============================================================================
# Copy this file to .env.local for local development.
# Configure these variables in Vercel Project Settings for Preview & Production.
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. APPLICATION ENVIRONMENT & URL
# ------------------------------------------------------------------------------
# Values: 'development' (local), 'preview' (Vercel Preview), 'production' (Vercel Production)
APP_ENV=development

# Base URL of the application.
# Local: http://localhost:3000
# Production: https://uklidsito.byzahr.app
APP_BASE_URL=http://localhost:3000

# ------------------------------------------------------------------------------
# 2. ADMINISTRATOR AUTHENTICATION (Google OAuth & NextAuth)
# ------------------------------------------------------------------------------
# Only this single Google account email will be allowed to log into the admin UI.
# Any other Google account will be strictly rejected.
ADMIN_EMAIL=your-admin-email@example.com

# Google Cloud OAuth 2.0 Client Credentials (from Google Cloud Console -> APIs & Services -> Credentials)
# Authorized redirect URI: <APP_BASE_URL>/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# Random 32+ character secret for NextAuth JWT encryption (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=generate-a-secure-random-secret-at-least-32-chars-long

# ------------------------------------------------------------------------------
# 3. GOOGLE SHEETS STORAGE SEPARATION (PROD vs TEST)
# ------------------------------------------------------------------------------
# In production (APP_ENV=production): GOOGLE_SHEET_ID_PROD is used.
# In development/preview (APP_ENV!=production): GOOGLE_SHEET_ID_TEST is used.
# The application will NEVER fall back to production data in non-production.
GOOGLE_SHEET_ID_PROD=your-production-google-spreadsheet-id
GOOGLE_SHEET_ID_TEST=your-test-google-spreadsheet-id

# Google Cloud Service Account for server-side Google Sheets & Drive API access
# Share both spreadsheets (PROD and TEST) with this service account email as Editor.
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-gcp-project.iam.gserviceaccount.com

# Service account private key (include entire string starting with -----BEGIN PRIVATE KEY-----)
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvgIBADANBgkqhkiG9w0BAQEFAASCBKgwggSkAgEAAoIBAQC...\n-----END PRIVATE KEY-----\n"

# ------------------------------------------------------------------------------
# 4. EMAIL SERVICE (Resend) & SAFETY LAYER
# ------------------------------------------------------------------------------
# Resend API Key (from https://resend.com/api-keys)
RESEND_API_KEY=re_your_resend_api_key

# Sender address (must be a verified domain in Resend or onboarding@resend.dev for initial testing)
EMAIL_FROM=UklidSiTo <uklid@uklidsito.byzahr.app>

# NON-PRODUCTION EMAIL REDIRECTION:
# In development and preview deployments, ALL task notification emails are redirected
# to this test recipient. Household members (Eva, Anna) will NEVER receive test emails.
TEST_EMAIL_RECIPIENT=your-test-inbox@example.com
